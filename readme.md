* For any of the variable you want to take as environment variable ,take it in as `os.eniron.get('value')`.
* Before running your app run the below given command.
`export value='value_which you want to give'`
* Now run the app.